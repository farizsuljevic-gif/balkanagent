BALKAN AGENT — v39 PRODUCTION RELEASE\n\nImplemented in this release:\n- D1-persisted leads table with indexes and public POST /api/leads endpoint.\n- Owner notification through Resend when a website/configurator lead is created.\n- Admin Leads CRM tab with server-side list, status workflow (new, contacted, qualified, demo, won, lost), and deletion.\n- Build Your AI Agent configurator for industry, goal, and channel selection; it creates a structured lead.\n- Expanded Solutions by Industry content for tourism, healthcare, real estate, e-commerce, and B2B services.\n- Substantive Privacy and Terms pages with contact, AI safety, bank-transfer, data-processing, and service-scope language.\n- Removed fabricated customer/activity metrics and removed unused presentation assets.\n- Existing BA branding, approved Balkan map, 11-language system, bank-transfer billing, authentication, invoices, customer PDF downloads, and existing bot fallback preserved.\n\nDeployment:\n1. Run schema.sql against the production D1 database (CREATE TABLE IF NOT EXISTS is safe for existing users/invoices).\n2. Deploy the full repository including functions/[[path]].js.\n3. Confirm production bindings/secrets: DB, SESSION_SECRET, ADMIN_PASSWORD, RESEND_API_KEY, INVOICE_FROM_EMAIL, OWNER_EMAIL.\n4. Test /api/leads, the public contact form, configurator, admin Leads CRM, and customer invoice download after deployment.\n\nImportant: external AI keys remain optional. Without them, the secure FAQ fallback remains active. WhatsApp/social production integrations still require official provider credentials and backend OAuth.\n
